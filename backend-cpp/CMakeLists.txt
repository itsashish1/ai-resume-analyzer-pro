cmake_minimum_required(VERSION 3.15)
project(resume_analyzer_cpp_api)
set(CMAKE_CXX_STANDARD 17)
add_executable(resume_analyzer_api src/main.cpp)
if (WIN32)
  target_link_libraries(resume_analyzer_api PRIVATE ws2_32)
endif()
